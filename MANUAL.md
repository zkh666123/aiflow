# FlowAI Studio 项目说明书

## 1. 项目定位

FlowAI Studio 是一个可视化 AI 工作流平台。

当前版本支持三种主要使用方式：

- 简单场景：固定工作流
- 复杂场景：在工作流中嵌入 `agent` 节点
- 调试场景：在编辑器中展示工作流状态和 Agent 执行路径

它不是一个单纯的聊天产品。
它的核心价值是把 AI 任务处理流程从后端代码中抽出来，变成可以配置、可以观察、可以复用的工作流。

## 2. 这个项目现在能做什么

当前项目可以用于：

- 用输入、LLM、RAG、工具、Agent、条件、输出等节点搭建工作流
- 管理知识库，并在聊天或工作流中使用知识库检索
- 管理工具，并在工作流中调用指定工具
- 执行工作流，并观察每个节点的运行状态
- 使用 Agent 节点在限定范围内自主决定是否调用工具、检索知识库或直接输出结果

适合的典型场景：

- 企业知识库问答
- 结构化信息抽取
- 带检索支持的内容生成
- 工具辅助任务处理
- AI 工作流演示和原型验证

## 3. 当前项目的整体结构

### 3.1 固定工作流

这是项目原本就具备的主要能力。

你手动决定：

- 哪个节点先执行
- 哪个节点后执行
- 是否检索知识库
- 是否调用工具
- 最终输出什么内容

这个模式适合稳定、可重复的流程。

### 3.2 工作流中嵌入 Agent

这是目前新增后的结构。

在工作流中，你可以插入一个 `agent` 节点。
节点前后仍然可以保持固定流程，但在这个节点内部，模型可以自己决定：

- 是不是直接输出结果
- 是否调用允许使用的工具
- 是否检索绑定的知识库
- 最多走多少步后停止

这个模式适合那些不容易完全提前写死的任务。

### 3.3 调试与轨迹观察

编辑器中的调试标签页现在可以展示：

- 工作流节点执行状态
- 每个节点返回的输出
- Agent 的逐步执行路径

这意味着画布不再只是一个流程编辑器，也变成了：

- 工作流编排界面
- Agent 控制界面
- 运行时观测面板

## 4. 主要页面入口

前端主要页面路由：

- `/apps`：应用列表
- `/apps/:appId/editor`：工作流编辑器
- `/knowledge-bases`：知识库管理
- `/tools`：工具管理
- `/debug`：调试中心

推荐使用路径：

1. 创建或选择一个应用
2. 打开应用编辑器
3. 在画布中搭建工作流
4. 保存并运行工作流
5. 在调试标签页中查看执行结果

## 5. 默认数据

执行种子数据后，项目会自动提供一组默认演示数据：

- 默认账号：`admin`
- 默认密码：`admin123`
- 一个默认知识库
- 一个默认演示应用
- 一个默认 RAG 演示工作流

推荐初始化命令：

```bash
cd flowai-studio-backend
npm install
cp .env.example .env
npx prisma db push
npx prisma db seed
npm run start:dev
```

```bash
cd flowai-studio-frontend
npm install
npm run dev
```

后端必须配置的环境变量：

- `DATABASE_URL`
- `JWT_SECRET`
- `QWEN_API_KEY`
- `QWEN_BASE_URL`

推荐的 `QWEN_BASE_URL` 配置：

```env
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
```

## 6. 工作流节点说明

### `start`

用于在工作流开始时初始化变量。

典型用途：

- 设置默认问题
- 设置固定参数
- 准备后续节点要用的上下文

### `userInput`

用于从工作流输入上下文中读取字段。

典型用途：

- 读取 `question`
- 读取 `text`
- 读取 `url`

### `llm`

用于执行一次普通模型调用。

典型用途：

- 分类
- 摘要
- 改写
- 结构化生成

### `rag`

用于执行知识库检索。

典型用途：

- 检索相关文档片段
- 在生成前补充知识背景

### `skill`

用于在工作流中直接调用某个指定工具。

当前内置工具类型包括：

- `time`
- `http`
- `json`
- `regex`

### `agent`

用于在固定工作流中引入一个半自主决策节点。

当前 Agent 节点支持：

- 配置任务目标
- 配置额外规则
- 可选绑定一个知识库
- 选择允许调用的工具
- 设置最大执行步数
- 输出执行轨迹

当前 Agent 节点还不能直接调用 MCP 工具。

### `condition`

用于计算条件表达式的真假结果。

当前需要特别注意：

- 这个节点现在可以计算布尔值
- 但它还不能真正驱动运行时动态分支跳转

也就是说，它现在更像“条件计算节点”，还不是完整意义上的“流程分支控制节点”。

### `output`

用于输出前面节点的结果。

常见写法：

- `{{llm_1.result}}`
- `{{rag_1.documents}}`
- `{{agent_1.result}}`

## 7. 工具管理

进入 `/tools` 页面可以管理工具。

当前前端页面最适合配置内置工具。
你可以创建一个内置工具，设置：

- 工具名称
- 工具描述
- 工具类型
- 内置类型
- 是否启用

当前限制：

- 虽然后端支持自定义工具配置字段，但前端页面目前没有完整暴露自定义配置表单
- 所以当前最顺手的使用方式，是优先使用内置工具

## 8. 知识库管理

进入 `/knowledge-bases` 页面。

你可以：

- 创建知识库
- 上传文档
- 删除文档
- 在聊天或工作流中使用知识库

当前推荐的文档格式：

- `.txt`
- `.md`
- `.json`
- 其他简单文本类文件

## 9. 调试与观测

当前有两个主要调试入口：

- `/debug`：快速测试 AI 聊天与直接运行工作流
- 应用编辑器中的“调试”标签页：查看节点状态和 Agent 轨迹

目前最有价值的调试入口，是编辑器中的调试页。

当 Agent 节点运行时，你可以看到：

- 第几步
- 当步思路
- 调用了什么工具
- 工具入参是什么
- 返回了什么观察结果
- 最终输出了什么

## 10. 推荐使用模式

### 模式 A：稳定工作流

适合逻辑明确、步骤固定的场景。

例如：

- 输入
- 检索
- LLM
- 输出

### 模式 B：固定工作流 + Agent

适合前后流程稳定，但中间某一步需要灵活判断的场景。

例如：

- 输入
- Agent
- 输出

或者：

- 输入
- 检索
- Agent
- 输出

### 模式 C：演示与原型验证

适合做这些展示：

- AI 编排
- Agent 路径规划
- 检索与工具调用结合
- 运行时轨迹透明化

## 11. 可直接照着跑的操作示例

下面这些示例可以直接作为演示案例使用。

---

### 示例 1：固定 RAG 工作流

目标：

- 从知识库中检索内容，并直接输出命中的文档片段

操作步骤：

1. 用 `admin / admin123` 登录
2. 打开默认演示应用
3. 进入工作流编辑器
4. 确认流程为：
   - `start`
   - `rag`
   - `output`
5. 运行工作流

预期结果：

- `rag` 节点返回命中的文档片段
- `output` 节点输出 `{{rag_demo.documents}}`

适合演示：

- 说明平台可以在没有 Agent 的情况下完成固定检索流程

---

### 示例 2：固定 LLM 摘要工作流

目标：

- 输入一段文本，让模型输出摘要

建议流程：

1. `userInput`
   - `inputField = text`
2. `llm`
   - `systemPrompt = 你是一个摘要助手`
   - `userPrompt = 请总结这段内容：{{userInput_1.text}}`
3. `output`
   - `outputValue = {{llm_1.result}}`

运行方式：

- 保存工作流
- 在编辑器中运行
- 后续如果你扩展输入表单，就可以直接传实际输入

适合演示：

- 说明平台可以做确定性的 AI 链式处理

---

### 示例 3：Agent 知识助手

目标：

- 让 Agent 自己判断是否要先检索知识库，再回答用户问题

建议流程：

1. `userInput`
   - `inputField = question`
2. `agent`
   - `goal = 回答用户问题；如果需要事实依据，优先检索知识库，再输出最终答案。输入：{{userInput_1.question}}`
   - `knowledgeBaseId = 默认知识库`
   - `availableSkillIds = []`
   - `maxSteps = 4`
   - `temperature = 0.2`
3. `output`
   - `outputValue = {{agent_1.result}}`

推荐的 Agent 规则：

```text
你是工作流中的智能助手。
如果问题涉及产品事实、功能说明或需要引用依据，请先使用知识库检索。
如果已有足够信息，直接输出最终答案。
回答要简洁、明确，不要编造。
```

预期结果：

- Agent 节点可能先调用 `knowledge_search`
- 然后基于检索结果输出最终答案
- 调试页中可以看到完整执行路径

适合演示：

- 展示固定工作流和半自主 Agent 的区别

---

### 示例 4：Agent + Regex 工具提取订单号

目标：

- 让 Agent 使用正则工具从用户文本中提取订单号

前置准备：

1. 打开 `/tools`
2. 创建一个内置工具
3. 建议配置：
   - 名称：`订单号提取器`
   - 类型：`builtin`
   - 内置类型：`regex`
   - 状态：启用

建议流程：

1. `userInput`
   - `inputField = text`
2. `agent`
   - `goal = 从用户输入中提取订单号；如果找到就返回订单号，没有就说明未找到。输入：{{userInput_1.text}}`
   - `availableSkillIds = [订单号提取器]`
   - `maxSteps = 3`
3. `output`
   - `outputValue = {{agent_1.result}}`

推荐的 Agent 规则：

```text
如果要提取订单号，请调用正则工具。
建议参数：
text 使用用户输入文本
pattern 使用 EC\\d{11,}
flags 使用 g
提取成功后直接输出订单号；失败则说明未找到。
```

示例输入：

```text
帮我看看订单 EC20260330003 现在是什么状态
```

预期结果：

- Agent 节点会调用正则工具
- 调试页中能看到工具入参和观察结果
- 最终输出会包含提取到的订单号，或者未找到的提示

适合演示：

- 展示 Agent 与工具调用的结合，而不依赖复杂外部系统

---

### 示例 5：Agent + Time 工具回答时间问题

目标：

- 让 Agent 遇到时间类问题时优先调用工具，而不是直接胡猜

前置准备：

1. 打开 `/tools`
2. 创建一个内置工具
3. 建议配置：
   - 名称：`当前时间工具`
   - 类型：`builtin`
   - 内置类型：`time`
   - 状态：启用

建议流程：

1. `userInput`
   - `inputField = question`
2. `agent`
   - `goal = 回答用户问题；如果问题涉及当前时间或日期，请调用时间工具。输入：{{userInput_1.question}}`
   - `availableSkillIds = [当前时间工具]`
   - `maxSteps = 2`
3. `output`
   - `outputValue = {{agent_1.result}}`

示例输入：

```text
现在是什么时间？
```

预期结果：

- Agent 会选择调用时间工具
- 编辑器调试页会展示工具调用轨迹
- 最终答案由工具结果生成

适合演示：

- 展示“基于工具的回答”而不是“直接模型猜答案”

## 12. 当前版本的真实限制

为了对项目描述保持真实，这里明确说明当前边界：

- `condition` 节点目前还不能真正控制动态分支跳转
- Agent 节点当前采用“模型返回 JSON 决策”的方式，不是原生 function-calling
- Agent 节点当前可以调已选 `skill` 和可选知识库，但还不能直接调 MCP 工具
- 前端工具管理页还没有完整暴露自定义工具配置能力
- 工作流输入表单能力目前还比较轻量，若要产品化还需要继续补

## 13. 推荐演示顺序

如果你要对外讲这个项目，推荐按下面顺序来展示：

1. 先展示知识库页面和默认数据
2. 再展示工具页面，并创建一个内置工具
3. 打开应用编辑器，创建一个固定工作流
4. 运行固定工作流，展示确定性执行
5. 再加入一个 Agent 节点
6. 再次运行，展示 Agent 自己决定是否检索或调工具
7. 最后打开调试页，逐步解释 Agent 实际做了什么

这个顺序能让项目故事比较完整：

- 第一层：平台能做固定 AI 工作流编排
- 第二层：平台能把 Agent 嵌进工作流中
- 第三层：平台还能把 Agent 的执行过程可视化展示出来
