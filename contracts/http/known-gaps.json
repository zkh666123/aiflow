{
  "schemaVersion": "1.0",
  "gaps": [
    {
      "method": "GET",
      "path": "/api/apps/:appId/share",
      "decision": "Go exposes GET in addition to the legacy POST, PATCH, and DELETE routes because the frontend reads current share settings."
    },
    {
      "method": "*",
      "path": "/api/workflow/templates/**",
      "decision": "Go exposes compatibility aliases while retaining /api/templates/** as the canonical public routes."
    }
  ]
}
